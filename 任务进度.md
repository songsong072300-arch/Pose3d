# 项目交接与任务进度

> 更新时间：2026-09-07（Asia/Shanghai）  
> 任务：在头戴相机视频中检测佩戴于左右手腕的两个同款运动相机，并估计每个相机的 6D 位姿  
> 目标物体：仅相机机身；手、腕带、支架和线缆均视为背景或遮挡物

---

## 一、当前状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| TripoSR 单图生成 3D 模型 | 已完成 | 已生成带 UV 和纹理的 OBJ/GLB |
| 模型标准化为 BOP | 已完成 | 已校正真实尺寸、中心原点和镜头方向 |
| 合成数据渲染 | 已暂停 | 正式集目标 2000 张，当前有 1012 张完整数据 |
| 八角点与 Heatmap 标签 | 已完成（当前数据） | 已生成 1012 个标签；正式渲染追加数据后需重新运行 |
| 角点网络与训练 | 未开始 | 计划输出 8 通道 Heatmap |
| 视频推理与 PnP | 未开始 | 多峰检测、实例分组和 PnP 尚待实现 |

当前没有后台渲染进程。正式数据位于：

```text
TripoSR/dataset/demo-bin-picking/train_pbr/
```

当前有效帧数：`1012`，若目标仍为 2000 张，剩余应生成：`988`。

状态检查：

```sh
cd /Users/song_sir/Code/编程考核

find TripoSR/dataset/demo-bin-picking/train_pbr \
  -type f -path '*/rgb/*' | wc -l

ps aux | grep '[r]ender_dataset.py'
```

---

## 二、输入资产与环境

| 资产 | 路径/说明 |
|---|---|
| 原始视频 | `head_left_rgb_raw.mp4` |
| 视频抽帧 | `frames/`，每 5 秒一张，共 20 张 |
| 建模产品图 | `样本.png`、`样本/样本2.png`；不是视频截图 |
| TripoSR 输出 | `TripoSR/output/camera_v1/0/` |
| 原始 OBJ | `TripoSR/output/camera_v1/0/mesh.obj` |
| 原始纹理 | `TripoSR/output/camera_v1/0/texture.png` |
| BOP 模型 | `TripoSR/dataset/demo-bin-picking/models/` |
| PBR 材质库 | `HCCEPose_src/cc0textures-512/`，1654 种材质 |
| TripoSR 环境 | conda `pose3d`，Python 3.10 |
| 渲染环境 | conda `render`，Python 3.11、bpy 4.5.9、numpy 1.26.4 |

产品图可以作为 TripoSR 输入，不要求来自视频，但应与视频中的设备型号和外形一致。单张正面产品图不能可靠重建背面，因此当前渲染器为背面增加了简化屏幕，详见第六节。

---

## 三、3D 模型标准化

脚本：`TripoSR/prepare_bop_model.py`

处理内容：

1. 读取 TripoSR 的 `mesh.obj` 和 `texture.png`。
2. 按三个轴分别缩放到实测尺寸。
3. 将包围盒中心移动到坐标原点，保证旋转围绕机身中心。
4. 六视图检查发现源模型镜头实际朝 `+X`，因此绕 Z 轴旋转 180 度，将项目镜头方向统一为 `-X`。
5. 导出 BlenderProc 兼容的 ASCII PLY并保留 UV/纹理声明。
6. 自动生成 BOP `models_info.json`。
7. 覆盖模型前创建时间戳备份。

最终坐标约定：

```text
X：机身厚度 32.8 mm，镜头朝 -X
Y：机身宽度 70.5 mm
Z：机身高度 44.2 mm
原点：机身包围盒中心
模型直径：76.9446 mm
```

最终包围盒：

```text
X = [-16.4, 16.4] mm
Y = [-35.25, 35.25] mm
Z = [-22.1, 22.1] mm
```

重新生成 BOP 模型：

```sh
cd /Users/song_sir/Code/编程考核
conda run -n pose3d python TripoSR/prepare_bop_model.py --origin center
```

模型六视图检查脚本：`TripoSR/debug_render_model.py`

已有检查结果：

```text
TripoSR/output/camera_v1/debug_glb/
TripoSR/output/camera_v1/debug_ply_normalized/
```

---

## 四、正式渲染数据设计

主脚本：`TripoSR/render_dataset.py`

根据 `frames/` 抽帧观察到：

- 两个同款相机通常同时出现，分别位于左右手腕附近。
- 相机主要位于画面下半部。
- 背面和侧面明显多于正面。
- 遮挡主要来自手指、手掌、衣物、腕带和线缆。
- 两个实例经常朝向不同方向。

当前正式参数：

```text
每帧实例：2 个 obj_id=1
图像尺寸：960 x 720
水平 FOV：82 度
Cycles samples：4
正面视角：20%
背面视角：20%
左右侧面：合计 60%（各 30%）
每实例遮挡总概率：55%
正式随机种子：20260907
```

> 视角比例说明：2026-09-15 起 `render_dataset.py` 的视角采样改为独立参数
> `--front-probability` / `--back-probability`（侧面 = 1 − 正面 − 背面，均分左右），
> 默认即 背:正:侧 = 1:1:3，依据真实视频以侧面视角为主的观察。
> 已有 1475 帧（2950 实例）的实际分布为 背面 51.3% / 正面 13.5% / 侧面 35.2%，
> 系旧参数（正面 10% + 剩余按 50% 背面）渲染，重新生成标签前注意两批数据分布不同。

遮挡采用分层随机策略：

```text
无遮挡：全部实例约 45%
轻遮挡：全部实例约 27.5%
中遮挡：全部实例约 19.25%
重遮挡：全部实例约 8.25%
```

遮挡体为一个随机长度、宽度、角度和位置的细长椭球，用于近似手指跨过机身。轻/中/重遮挡大致覆盖机身的 10-20%、20-35%、35-55%。重遮挡比真实视频多数帧更困难，但比例较低；保留无遮挡和轻遮挡是为了让网络先学到稳定的机身几何。

最终难度验收结果保存在：

```text
TripoSR/dataset/demo-bin-picking/train_pbr.final-difficulty-validation-20260907/
```

正式数据只使用无后缀的 `train_pbr/`。

---

## 五、渲染器已修复的问题

- 原实现将目标悬空 3-20 cm，已移除。
- 原实现使用 `uniformSO3()` 任意旋转，已改为背面/侧面为主的受限分布。
- 模型旋转原点原在包围盒最小角，已改为机身中心。
- 实际源模型镜头朝 `+X`，已规范为 `-X`。
- 二进制 PLY 被 BlenderProc 当文本重写后损坏，已改为 ASCII PLY。
- Blender 4.5 旧删除 API 失效，已改用 `bpy.data.objects.remove(..., do_unlink=True)`。
- 每轮添加新相机关键帧会重复渲染，已固定每轮只使用 frame 0。
- BlenderProc 的 BOP writer 默认启动 4 个子进程，在 macOS 下会因 `_bpy` 导入失败而挂起。当前默认关闭 mask/info/COCO；需要时使用 `--bop-masks`，并强制单进程。
- `camera.json` 现在每次按命令参数写入，避免静默使用旧内参。
- 两个 BOP 模型实例只加载一次，每帧更新位置和旋转，避免缓存引用已删除对象。
- 场景元数据支持追加，帧号与 BOP 图像编号一致。

控制台可能出现：

```text
Warning! Detected output entries with duplicate keys and paths
```

这是循环复用 BlenderProc 输出注册项产生的警告。已经验证每轮只新增一张 RGB、一张深度图和一组 JSON 标注，不影响数据一致性。

---

## 六、背面屏幕处理与限制

真实视频大量出现相机背面，而 TripoSR 输入只展示正面。生成网格的原始背面比较模糊，因此渲染器在本地 `+X` 面增加：

- 黑色屏幕边框；
- 深色显示区域。

屏幕代理保持在 `32.8 mm` 机身厚度包围盒内。它改善了背面轮廓，但不是实际多视图重建结果，不包含真实 UI、标签或按钮细节。

正式训练前后应注意这个领域差异。若真实视频效果较差，优先补充真实背面照片并重建/修整模型，而不是单纯增加更多当前合成图。

---

## 七、正式集恢复方式

正式集目标为 2000 张，当前有 791 张，因此恢复时追加 1209 张。必须换用新随机种子，避免与前 791 张场景重复。

```sh
cd /Users/song_sir/Code/编程考核/TripoSR/dataset/demo-bin-picking

/Users/song_sir/miniconda3/envs/render/bin/python \
  ../../render_dataset.py 384 \
  ../../../HCCEPose_src/cc0textures-512 \
  --seed 20260923 \
  --samples 4 \
  --front-probability 0.10 \
  --back-probability 0.10 \
  --occlusion-probability 0.55 \
  > /tmp/camera_pose_render_remaining.log 2>&1
```

> 当前 1616 张（旧分布 1475 + 侧倾 141）。补渲染批次用 10/10/80（正/背/侧）
> 与既有 141 张侧倾帧一致；补齐 2000 张后总分布约 背 40% / 正 13% / 侧 47%，
> 剩余偏差可在训练端用 WeightedRandomSampler 按 `scene_metadata.json` 的
> `view_modes` 补偿。
>
> 2026-09-15 注：曾试验第一人称视角重构（头高俯视、画面外手臂、2.75x 模型缩放），
> 评估后放弃；渲染脚本已还原至本节所述几何。模型缩放同步机制
> （render_config.json）保留在标签/推理脚本中，无该文件时自动退回 1.0 倍。

监控：

```sh
tail -f /tmp/camera_pose_render_remaining.log

find /Users/song_sir/Code/编程考核/TripoSR/dataset/demo-bin-picking/train_pbr \
  -type f -path '*/rgb/*' | wc -l
```

如果再次中断，应重新计算：

```text
剩余数量 = 2000 - 当前 RGB 文件数量
```

然后再次换一个未使用的随机种子。不要删除无后缀的 `train_pbr/`，脚本会自动追加，并在第 1000 张后创建 BOP 分块 `000001/`。

正式输出结构：

```text
TripoSR/dataset/demo-bin-picking/
├── camera.json
├── models/
│   ├── obj_000001.ply
│   ├── obj_000001.png
│   └── models_info.json
└── train_pbr/
    ├── scene_metadata.json       # 每帧视角类型与遮挡等级
    ├── 000000/
    │   ├── rgb/*.jpg
    │   ├── depth/*.png
    │   ├── scene_gt.json         # 两实例 R/t，平移单位 mm
    │   └── scene_camera.json     # K 与相机外参
    └── 000001/                   # 第 1001-2000 张
```

---

## 八、八角点与 Heatmap 标签

标签脚本：`TripoSR/generate_corner_heatmaps.py`

输入：

- `models/models_info.json` 中的 3D 包围盒八角点；
- `scene_gt.json` 中每个实例的 `R/t`；
- `scene_camera.json` 中的相机内参 `K`。

输出：

```text
corner_labels/
├── annotations.json       # 原图尺度下两个实例的 8 个二维角点
├── splits.json            # 80% train / 10% val / 10% test
└── heatmaps/*.npz         # 每张图 (8, 180, 240)，uint8
```

同一角点通道包含两个高斯峰，对应同帧两个相机实例。工具已在 20 张验证数据上通过：8 个通道都有有效峰值，标签数量与 RGB 数量一致。

当前 1012 张数据已运行完成；若后续补齐正式 2000 张，需重新运行：

```sh
cd /Users/song_sir/Code/编程考核
conda run -n pose3d python TripoSR/generate_corner_heatmaps.py
```

角点投影检查脚本：`TripoSR/validate_bop_annotations.py`

```sh
conda run -n pose3d python TripoSR/validate_bop_annotations.py
```

该脚本将两个实例的 3D 包围盒投影回 RGB。已有验证图显示两组线框能够准确包围对应机身，说明 `3D 角点 -> R/t -> K -> 像素` 链路正确。

---

## 九、正式渲染完成后的检查

1. 确认 RGB、深度、`scene_gt` 和 `scene_camera` 均为 2000 帧。
2. 确认每帧 `scene_gt` 包含两个 `obj_id=1` 实例。
3. 统计 `scene_metadata.json` 的正面、背面、侧面和遮挡等级分布。
4. 随机抽查各视角及各遮挡等级。
5. 运行 `generate_corner_heatmaps.py`。
6. 检查 Heatmap 文件数量与 RGB 数量一致、形状 `(8, 180, 240)` 和每通道峰值。
7. 当前 1012 张时 `splits.json` 为 train=809、val=101、test=102；补齐到 2000 张后应为 train=1600、val=200、test=200。
8. 完成以上检查后再实现训练数据加载器和角点网络。

---

## 十、主要脚本

| 文件 | 用途 |
|---|---|
| `TripoSR/prepare_bop_model.py` | OBJ 缩放、方向规范、PLY 导出、models_info 生成 |
| `TripoSR/debug_render_model.py` | GLB/PLY 六视图检查 |
| `TripoSR/render_dataset.py` | 正式 BOP 合成数据渲染 |
| `TripoSR/validate_bop_annotations.py` | 3D 包围盒投影可视化 |
| `TripoSR/generate_corner_heatmaps.py` | 生成八角点、Heatmap 和数据划分 |

---

## 十一、仍需解决的风险

1. 背面屏幕是简化代理，不是实际背面重建。
2. 遮挡椭球只能模拟遮挡面积和肤色，不能替代真实手指形状。
3. 背景来自 CC0 PBR 材质，包含布料、木材和石材等，但不完全复刻叠衣视频。
4. 当前相机内参按 82 度水平 FOV 推导；最终 PnP 平移精度需要真实头戴相机标定。
5. 多实例角点分组尚未实现。每通道有两个峰，推理时必须将八类角点正确分成左右两个实例。
6. 所有带 `train_pbr.*` 后缀的目录均为调试、失败或验收结果，不得混入正式训练。

下一步：如需达到 2000 张，追加渲染剩余 988 张并重新生成 Heatmap；随后实现训练数据加载器和角点网络。
