# 角点 Heatmap 标签生成详解

> 对应脚本：`TripoSR/generate_corner_heatmaps.py`（本项目自制，非开源自带）
> 上游输入：`train_pbr/` 合成渲染数据 + `models/models_info.json`
> 下游输出：`corner_labels/`（训练网络所需的全部标签与数据划分）

---

## 第一部分：为什么要生成 Heatmap 标签

### 1.1 它在管线中的位置

```text
TripoSR 单图建模 → BOP 渲染数据(R/t/K 真值) → 【本脚本】 → 角点网络训练 → 真实视频推理 + PnP
                                                    ↑
                                          训练的"标准答案"在这一步诞生
```

监督学习需要成对的 `(输入, 目标)`。输入是 RGB 图，而"目标"必须由这份脚本来制造——
渲染数据本身存的是 3D 位姿（`scene_gt.json` 里的 R/t）和相机内参（`scene_camera.json` 里的 K），
它们**不能直接喂给网络**，必须先换算成和网络输出同一种形式的东西。

### 1.2 网络到底在学什么

设计中的网络输出是 **8 通道热图**，每个通道对应 3D 包围盒的一个角点：

```text
通道 0 ──回答──> "角点0（min_x,min_y,min_z）投影在图像哪里？"
通道 1 ──回答──> "角点1（min_x,min_y,max_z）投影在图像哪里？"
...
通道 7 ──回答──> "角点7（max_x,max_y,max_z）投影在图像哪里？"
```

每张热图是一个与输入图像**空间对齐**的概率图：角点所在位置亮度高（高斯亮斑），
其余位置接近 0。训练时对比"预测热图"和"真值热图"逐像素计算 MSE/Focal Loss；
推理时对预测热图做 `argmax` 即得到 8 个角点的像素坐标，再交给 PnP 解算 6D 位姿。

所以"生成标签"就是：**把数学上已知的角点 3D 坐标，画成网络要学的那个样子（高斯斑）**。

### 1.3 为什么用热图，而不是直接回归 16 个坐标数字

直接回归（网络输出 8×2=16 个数字）也是合法做法，但本项目选热图，原因按重要性排序：

**（1）双实例无序问题（本项目最关键）**

每帧渲染了 2 个同款相机。直接回归必须指明"这 16 个数属于第几个实例"，
但两个相机外观完全相同，"谁是 1 谁是 2"没有稳定定义（左右腕可互换、
前后关系随视角变化），训练标签会自相矛盾。

热图天然回避：**同一通道直接画 2 个峰**（两实例各一个），训练时根本不需要指明归属。
"哪个峰属于哪个实例"的分组难题推迟到推理阶段再解决（项目文档第十一节风险 5）。

**（2）学习难度：局部判断 vs 全局压缩**

- 直接回归：把整张图的信息压缩成 16 个数字，网络必须理解全局空间布局，难学；
- 热图：每个像素只需回答"我这里是不是角点"——与卷积的局部感受野天然匹配，
  是 FCOS、人体姿态估计（Stacked Hourglass / SimpleBaseline）等密集预测框架的通行做法。

**（3）遮挡鲁棒性**

角点被手指挡住时，直接回归的 L2 loss 只能强行让网络输出一个数字；
热图允许网络从周围机身轮廓"推断"角点应在的位置，且峰高天然提供置信度。

**（4）损失信号更精细**

角点偏 3 像素和偏 30 像素，热图逐像素对比下梯度差异明显；回归 16 个数字时
区分度小得多。

**代价**：网格量化误差——热图分辨率为原图 1/4（240×180），角点坐标最多偏差约 4 像素。
但 PnP 用 8 个角点联合求解，量化误差互相抵消，不会直接转化为位姿误差。

### 1.4 为什么合成数据的标签是"绝对准确的"

真实数据做角点标注需要人工点选，误差 2~5 像素起步，且两个同款相机极易标混。
而本项目的合成数据里：

```text
3D 角点坐标 (models_info.json，来自实测尺寸的包围盒)
× 精确位姿 R/t (scene_gt.json，Blender 渲染时记录)
× 精确内参 K (scene_camera.json)
────────────────────────────────────
= 数学上精确的角点像素坐标，零标注误差
```

已验证投影链路正确：3D 包围盒投影与深度图 100% 一致（1012 帧全量检查通过）。

### 1.5 标签的具体设计参数

| 参数 | 值 | 理由 |
|---|---|---|
| 热图尺寸 | 240×180（原图 1/4） | 匹配 backbone 下采样步长；省显存 |
| 高斯 σ | 2.5（热图像素）≈ 10 原图像素 | 峰太尖难学（监督信号稀疏），太胖则定位不精 |
| 存储 | uint8（×255 量化） | 比 float32 省 75% 磁盘，量化误差 ~0.002 远小于 σ |
| 每通道峰数 | 2（双实例） | 见 1.3（1） |

---

## 第二部分：脚本实现逻辑逐段讲解

脚本共 131 行，结构非常干净：3 个工具函数 + 1 个主流程。

### 2.1 命令行参数（15-24 行）

```python
--heatmap-width 240 / --heatmap-height 180   # 热图尺寸，原图 960×720 的 1/4
--sigma 2.5                                 # 高斯标准差（热图像素单位）
--seed 2026                                 # 数据划分随机种子（保证可复现）
dataset                                      # BOP 数据集路径（有默认值）
```

### 2.2 `bbox_corners()`：八角点定义与通道顺序（27-34 行）

```python
for x in (min_x, max_x)
  for y in (min_y, max_y)
    for z in (min_z, max_z)
```

X 变化最慢、Z 变化最快（二进制计数顺序），得到 8 个角点的**稳定排列**：

```text
通道 0 = (min_x, min_y, min_z)   通道 4 = (max_x, min_y, min_z)
通道 1 = (min_x, min_y, max_z)   通道 5 = (max_x, min_y, max_z)
通道 2 = (min_x, max_y, min_z)   通道 6 = (max_x, max_y, min_z)
通道 3 = (min_x, max_y, max_z)   通道 7 = (max_x, max_y, max_z)
```

**为什么顺序很重要**：训练时网络学的是"通道 i ↔ 3D 角点 i"的固定对应；
推理时 PnP 需要把"2D 峰位置"和"3D 角点坐标"一一配对。顺序一旦漂移，
位姿会解算出完全错误的结果。这个顺序被写进 `annotations.json` 的
`corner_channel_order_mm` 字段，供训练与推理端共同遵守。

### 2.3 `project()`：3D→2D 投影（37-43 行）

```python
points_camera = corners @ R.T + t        # 模型坐标系 → 相机坐标系
homogeneous  = points_camera @ K.T       # 相机坐标 → 齐次像素坐标
pixels       = homogeneous[:, :2] / homogeneous[:, 2:3]   # 齐次除法
```

- `cam_R_m2c` / `cam_t_m2c`：BOP 惯例，model-to-camera 变换，t 单位 mm；
- 这三行就是针孔相机模型的矩阵形式，与渲染端（BlenderProc）互为逆过程；
- 同时返回每个角点的相机坐标 Z（`depths`），供下一步有效性判断用。

### 2.4 `draw_gaussian()`：局部高斯绘制（46-56 行）

```python
radius = ceil(3.0 * sigma)                # 只画 3σ 邻域（外面积≈0，不值得算）
patch  = exp(-((xx-x)² + (yy-y)²) / 2σ²) # 二维高斯公式
heatmap[y0:y1, x0:x1] = np.maximum(旧值, patch)   # 关键：取最大，不是相加
```

**为什么用 `np.maximum` 而不是 `+=`**：两个实例的同类角点可能投影到相邻位置，
高斯会重叠。相加会让重叠处 >1.0（爆值），且相当于变相改变了"两个独立目标"的语义；
取最大则保持每处概率 ≤1，语义是"这里离某个角点最近有多近"。经验证每通道恰好
检测出 2 个独立峰，无粘连。

中心坐标是**连续值**（不取整），峰顶像素值理论最低约 245/255（中心恰在像素间时），
这是正常现象而非 bug。

### 2.5 主流程 `main()`（59-127 行）

**遍历结构（69-72 行）**：

```python
glob("[0-9][0-9][0-9][0-9][0-9][0-9]")   # 只匹配 000000/000001 等 BOP 分块目录
```

正则式匹配保证了 `scene_metadata.json`、`corner_labels/` 等非数字目录不会被误读。

**每帧处理（73-109 行）**，对每个实例依次执行：

```text
1. project() 得到 8 个角点的原图像素坐标 + 深度
2. in_frame 判断（84-86 行）：
     0 ≤ x < 960  且  0 ≤ y < 720  且  depth > 0
   ── depth > 0 的坑：相机后方的点做齐次除法后坐标会镜像翻转，
      可能"恰好"落进画面内，若不排除会画出完全错误的峰
3. 坐标 × 0.25 缩放到热图尺度（87-88 行）
4. 逐通道 draw_gaussian（89-91 行）
```

出画面的角点不画峰（`corners_in_frame` 记录了每个角点的有效性），
这是 PnP 阶段只使用可见角点求解的依据。

**标签落盘（99-101 行）**：

```python
npz = rint(heatmap × 255).astype(uint8)   # float[0,1] → uint8[0,255]
```

**元数据 `annotations.json`（102-109, 121-124 行）** 每帧记录：

- `image` / `heatmap`：RGB 与 npz 的相对路径（数据加载器按此索引）；
- `corners_2d`：原图尺度下两实例的 8 角点像素坐标（回归式评估、PnP 真值都用它）；
- `corners_in_frame`：角点可见性；
- `corner_channel_order_mm`：通道 ↔ 3D 角点对应表（见 2.2）。

**数据划分 `splits.json`（111-120 行）**：

```python
rng.shuffle(records)          # seed=2026 固定打乱，可复现
train 80% / val 10% / test 10%
```

当前 1012 帧 → train 809 / val 101 / test 102。val 用于训练中调参与早停，
test 只在最终评估时用一次。

### 2.6 输出产物结构

```text
corner_labels/
├── annotations.json    # 通道顺序定义 + 每帧角点坐标与可见性
├── splits.json         # train/val/test 的帧 id 列表
└── heatmaps/
    └── 000000_000000.npz   # 命名 = 分块号_帧号，内容 (8,180,240) uint8
```

---

## 第三部分：已完成的验证与结论

1. **抽样功能验证**（5 帧，覆盖正/背面、无/中/重遮挡、两批次）：直接 import
   本脚本的函数运行——每通道恰好 2 峰、峰位与投影角点偏差 ≤0.55px、数值范围正确；
2. **上游投影链路验证**（1012 帧全量）：3D 包围盒多边形与深度图 100% 一致；
3. 可视化样例见 `heatmap预览/`（含峰放大版，高斯形状清晰）。

结论：脚本逻辑正确、标签可用，可对全量 1012 帧正式执行：

```sh
cd /Users/song_sir/Code/编程考核
conda run -n pose3d python TripoSR/generate_corner_heatmaps.py
```

> 注意：若之后补渲染更多帧，需重新运行本脚本（splits 会随总数变化重新划分）。
