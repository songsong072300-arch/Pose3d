# 八角点检测端到端调用链

## 目录职责

```text
pose3d/
├── common/                 # 训练与推理共同调用
│   ├── model.py            # CornerHeatmapNet
│   └── image.py            # ImageNet 归一化、Letterbox
├── train/                  # 仅训练阶段调用
│   ├── dataset.py          # 单实例 ROI 和 Heatmap 标签
│   ├── losses.py           # Heatmap + 坐标双损失
│   ├── train_heatmap.py    # 角点网络训练入口
│   └── export_yolo_labels.py
└── inference/              # 仅推理阶段调用
    ├── pipeline.py         # YOLO、ROI、提峰和坐标逆变换
    ├── predict_image.py    # 单图入口
    ├── run_video.py        # 视频入口
    └── detect_image.py     # 单独检查 YOLO
```

## 训练调用链

入口：

```bash
conda run -n pose3d python -m pose3d.train.train_heatmap \
  --root TripoSR/dataset/demo-bin-picking \
  --epochs 30 --batch-size 8 --workers 4 --amp \
  --out checkpoints/dino_corner_net.pt
```

实际调用：

```text
train/train_heatmap.py
  -> train/dataset.py:RoiCornerDataset
       -> common/image.py:letterbox_image + image_to_tensor
  -> common/model.py:CornerHeatmapNet
  -> train/losses.py:corner_heatmap_loss
  -> 保存解码器 checkpoint
```

训练不调用 `pose3d/inference/`。

## 推理调用链

单图入口：

```bash
conda run -n pose3d python -m pose3d.inference.predict_image \
  --image frames/frame_00000s.jpg \
  --detector checkpoints/dji_action4_yolo.pt \
  --corner-weights checkpoints/dino_corner_net.pt
```

视频入口：

```bash
conda run -n pose3d python -m pose3d.inference.run_video \
  --video head_left_rgb_raw.mp4 \
  --detector checkpoints/dji_action4_yolo.pt \
  --corner-weights checkpoints/dino_corner_net.pt
```

实际调用：

```text
inference/predict_image.py 或 inference/run_video.py
  -> inference/pipeline.py:VideoCornerPipeline
       -> YOLO 检测两个实例
       -> common/image.py:letterbox_image + image_to_tensor
       -> common/model.py:CornerHeatmapNet
       -> inference/pipeline.py:extract_peaks
       -> 角点逆变换回原图
  -> 绘制 8 个有序角点
```

推理不调用 `pose3d/train/`，也不执行 PnP。

## YOLO 数据准备

YOLO 检测器属于角点网络之前的实例分离阶段。其标签导出命令为：

```bash
conda run -n pose3d python -m pose3d.train.export_yolo_labels
```

生成的数据用于单独训练 YOLO，不进入 `CornerHeatmapNet` 的角点损失。
