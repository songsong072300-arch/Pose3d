# 端到端调用链

> 从"一张产品图"到"视频帧上的 8 个角点"的完整链路：每环的命令、函数级调用关系、当前状态。
> 更新：2026-09-17（对应提交 `298f49c`）
> 目标：**角点检测**（网络输出 8 通道热图 → 提峰得到 8 个角点；PnP/位姿为可选后缀）

---

## 〇、全链路总览

```text
━━━━ 阶段 A：数据生成（已完成，可追加）━━━━
产品图 ──TripoSR──> mesh.obj ──prepare_bop_model──> BOP 模型(models/)
                                                    │
                                    render_dataset.py（BlenderProc）
                                                    ▼
                       train_pbr/：RGB + depth + scene_gt + scene_camera
                                                    │
                          generate_corner_heatmaps.py
                                                    ▼
                    corner_labels/：annotations + splits + npz 热图
                              │                         │
              export_yolo_labels.py                训练数据就绪
                              ▼
                    checkpoints/yolo_dataset/（YOLO 格式检测标签）

━━━━ 阶段 B：训练（游戏本）━━━━
┌─────────────────────────────┐  ┌──────────────────────────────┐
│ 角点网络 train_heatmap.py    │  │ 检测器 YOLO（ultralytics）    │
│  --arch scratch 或 dinov2    │  │  yolo train（约几分钟）        │
└─────────────┬───────────────┘  └──────────────┬───────────────┘
              ▼                                 ▼
   checkpoints/boxdreamer_roi*.pt     checkpoints/dji_action4_yolo.pt
   （含 val corner_err、arch）        （runs/detect/.../best.pt 拷贝）

━━━━ 阶段 C：推理（真实视频）━━━━
视频帧 ──YOLO 检测──> 前 2 框 ──裁剪+预处理──> 角点网络
      ──> 8 通道热图 ──extract_peaks──> 8 角点（小图系）
      ──坐标逆映射──> 原图系 8 角点 ★交付
      ──(可选 solvePnP)──> 6D 位姿 ──(可选 EMA)──> 平滑
```

---

## 一、阶段 A：数据生成链

### A1. 单图建模 → BOP 标准化（✅ 已完成）

```bash
# TripoSR 推理（一次性）
conda run -n pose3d python TripoSR/run.py ...        # 输出 mesh.obj + texture.png

# 标准化为 BOP：实测尺寸 32.8×70.5×44.2mm、原点居中、镜头朝 -X
conda run -n pose3d python TripoSR/prepare_bop_model.py --origin center
# 产物: models/obj_000001.ply + models_info.json
```

### A2. 合成渲染（✅ 1616/2000 张，可追加）

```bash
cd TripoSR/dataset/demo-bin-picking
/Users/song_sir/miniconda3/envs/render/bin/python \
  ../../render_dataset.py 384 ../../../HCCEPose_src/cc0textures-512 \
  --seed 20260923 --samples 4 \
  --front-probability 0.10 --back-probability 0.10 \
  --occlusion-probability 0.55
```

调用链（`render_dataset.py`）：

```text
main()
 ├ wrist_camera_rotation()   按视角模式(正/背/侧)定向机身，含扰动
 ├ add_wrist_context()       前臂/护腕/手掌/手指/线缆代理（不写入标注）
 ├ create_segment()          两点间胶囊圆柱
 ├ 遮挡椭球                   分级 轻/中/重 遮挡
 └ bproc.writer.write_bop()  RGB+depth+scene_gt+scene_camera
```

状态：1475 旧分布（背 51%）+ 141 侧倾（10/10/80）；补 384 张后总分布约背 40/正 13/侧 47。

### A3. 角点热图标签（⚠️ 需在数据追加后重跑）

```bash
conda run -n pose3d python TripoSR/generate_corner_heatmaps.py
```

调用链（`generate_corner_heatmaps.py`）：

```text
main()
 ├ bbox_corners()   models_info → 8 个 3D 角点（XYZ 二进制计数序=通道序）
 ├ project()        3D角点 × R/t(来自scene_gt) × K(来自scene_camera) → 像素
 ├ draw_gaussian()  角点处画高斯峰（ROI单峰版在数据集内现场画）
 └ 产出 corner_labels/{annotations.json, splits.json, heatmaps/*.npz}
```

> 注意：重跑会**按当前全部帧数重新划分** splits（1616 → train 1292/val 161/test 161 级别）。

### A4. YOLO 检测标签导出（✅ 已导出 1475 张；A3 重跑后再跑一次可覆盖 1616）

```bash
conda run -n pose3d python -m pose3d.export_yolo_labels
# 产物: checkpoints/yolo_dataset/{images,labels,data.yaml}
# 原理: annotations.json 里每实例 corners_2d 的外接矩形 = 免费的检测框真值
```

---

## 二、阶段 B：训练链（游戏本执行）

### B1. 环境准备

```bash
git clone https://github.com/songsong072300-arch/Pose3d.git && cd Pose3d
conda create -n pose3d python=3.10 && conda activate pose3d
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126  # 按CUDA版本
pip install -r requirements-pose3d.txt
pip install ultralytics      # 检测器训练/推理
```

### B2. 角点网络训练（主线）

```bash
# A 组：从零小 Transformer（快，先验证训练闭环）
python -m pose3d.train_heatmap --roi --arch scratch \
  --epochs 30 --batch-size 16 --workers 4 --amp --seed 2026 \
  --out checkpoints/boxdreamer_roi.pt

# B 组：冻结 DINOv2-Base + 解码头（论文方案，抗域差距）
python -m pose3d.train_heatmap --roi --arch dinov2 --dinov2-variant base \
  --epochs 30 --batch-size 16 --workers 4 --amp --seed 2026 \
  --out checkpoints/boxdreamer_roi_dinov2.pt
```

函数级调用链（`train_heatmap.py`）：

```text
main()
 ├ RoiPoseDataset("train"/"val")            roi_pipeline.py
 │   └ __getitem__(): 读整图→按实例角点裁剪→resize 240×180→归一化
 │                    →角点换算 q→现场画单峰高斯
 ├ build_net(arch)                          boxdreamer_single.py
 │   ├ BoxDreamerSingle:  patchify→6层Transformer→unpatchify→sigmoid (5.42M)
 │   └ DinoCornerNet:     冻结DINOv2→patch tokens→卷积解码头 (训1.2M)
 ├ heatmap_loss()                           前景加权 SmoothL1 (1+100×target)
 ├ corner_error()                           val 角点像素误差（直接量化目标）
 ├ CosineAnnealingLR + AdamW + AMP
 └ torch.save({model, arch, dinov2_variant, val_loss, val_corner_err})
```

**验收标准**：`corner_err` 收敛到个位数像素（热图 1/4 分辨率下 4px≈原图 16px，PnP 联合解算可抵消大部分量化）。

### B3. YOLO 检测器训练（几分钟）

```bash
yolo detect train model=yolov8n.pt \
  data=checkpoints/yolo_dataset/data.yaml \
  epochs=30 imgsz=960 batch=16 device=0
cp runs/detect/train/weights/best.pt checkpoints/dji_action4_yolo.pt
```

### B4. 分步验收（训练后）

```bash
# 单图检测体检（只跑 YOLO 环节）
python -m pose3d.detect_image --image frames/frame_00000s.jpg \
  --weights checkpoints/dji_action4_yolo.pt --out outputs/det.jpg
```

---

## 三、阶段 C：推理链（端到端视频）

```bash
python -m pose3d.run_video \
  --video head_left_rgb_raw.mp4 \
  --detector checkpoints/dji_action4_yolo.pt \
  --corner-weights checkpoints/boxdreamer_roi_dinov2.pt \
  --camera TripoSR/dataset/demo-bin-picking/camera.json \
  --out outputs/pose_result.mp4
```

函数级调用链（`run_video.py → roi_pipeline.py`）：

```text
run_video.main()
 ├ 读 camera.json → K；models_info(+render_config) → 3D角点
 └ VideoPosePipeline（每帧 __call__）
    ├ _detect():        YOLO → xyxy+conf → 置信度前 2 框
    ├ 裁剪:              框外扩15% → BGR→RGB → resize 240×180 → 同训练的归一化
    ├ net(x):            角点网络 → (8,180,240) 热图
    │                     └ ck 含 arch 字段 → 自动重建 scratch/dinov2 对应网络
    ├ extract_peaks():   每通道 minMaxLoc + 3×3 加权质心 → 亚像素角点
    │                     └ 峰<0.15 → NaN（角点不可见）
    ├ 坐标逆映射:         ×(裁剪尺寸/240)+偏移 → 原图像素坐标 ★8角点交付
    ├ solvePnPRansac():  (可选) 3D角点+K → rvec/tvec，≥4 有效角点才解
    └ EMATracker:        (可选) 位姿时序平滑
 └ draw_results():       画框+角点+深度 → 写出 mp4
```

---

## 四、当前状态与依赖图

| 环节 | 状态 | 产物 | 依赖 |
|---|---|---|---|
| A1 建模/标准化 | ✅ | models/ | — |
| A2 渲染 | ✅ 1616 张（可补 384） | train_pbr/ | A1 |
| A3 热图标签 | ⚠️ 覆盖 1475（重跑覆盖 1616） | corner_labels/ | A2 |
| A4 YOLO 标签 | ✅（A3 后重跑覆盖） | yolo_dataset/ | A3 |
| B2 角点网络训练 | ❌ 待游戏本 | boxdreamer_roi*.pt | A3 |
| B3 YOLO 训练 | ❌ 待游戏本 | dji_action4_yolo.pt | A4 |
| C 视频推理 | ❌ 待 B2+B3 | 角点/位姿视频 | B2+B3 |

## 五、已知风险与提升点

1. **合成→真实域差距**：首选 `--arch dinov2`（冻结预训练特征抗域偏移）
2. 141+384 张新帧无热图标签：A3 重跑即覆盖（splits 会重划）
3. fine 损失（论文 2.0·L_fine 角点坐标项）未实现——数据已备好（`corners` 字段）
4. YOLO 若在真实视频误检：备选 Grounding DINO 零样本检测（论文 demo 同款）
5. 相机内参为 82°FOV 推导值：只影响可选的 PnP 精度，不影响角点输出
