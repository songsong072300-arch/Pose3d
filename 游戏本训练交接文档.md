# Pose3D 游戏本训练交接文档

> 更新日期：2026-09-18  
> 代码仓库：<https://github.com/songsong072300-arch/Pose3d.git>  
> 已推送的基线提交：`f4719d7 Refine and separate corner detection pipeline`

## 1. 迁移目标

本次只把角点网络的训练和后续推理迁移到 NVIDIA 游戏本。TripoSR 建模和 Blender 合成数据渲染已经完成当前阶段的输入准备，不是启动训练的前置环境。

最终任务链路为：

```text
RGB 图像
  -> YOLO 分离最多两个相机实例
  -> 每个 ROI 输入同一个共享角点网络
  -> 8 x 168 x 224 Heatmap
  -> 每个实例的 8 个有序二维角点
```

本任务在二维角点输出处结束，不运行 PnP，不输出 6D 位姿。

## 2. 当前网络和代码入口

网络结构已固定为：

```text
DINOv2-Small（冻结）
  -> 最后四层中间特征
  -> 通道投影和四层特征融合
  -> 两级上采样解码器
  -> 8 通道 Heatmap
```

总参数约 `23.21M`，冻结 DINOv2 后可训练参数约 `1.157M`。训练损失为：

```text
L = weighted SmoothL1 heatmap loss + 2.0 * coordinate loss
```

| 用途 | 路径 |
|---|---|
| 共享网络 | `pose3d/common/model.py` |
| 图像预处理 | `pose3d/common/image.py` |
| 训练数据集 | `pose3d/train/dataset.py` |
| 损失函数 | `pose3d/train/losses.py` |
| 训练入口 | `pose3d/train/train_heatmap.py` |
| YOLO 标签导出 | `pose3d/train/export_yolo_labels.py` |
| 单图推理 | `pose3d/inference/predict_image.py` |
| 视频推理 | `pose3d/inference/run_video.py` |

## 3. 当前数据状态

Mac 当前数据目录约 `238 MB`，状态如下：

| 内容 | 数量 | 说明 |
|---|---:|---|
| RGB | 1616 | 角点训练的图像输入 |
| Depth | 1616 | 当前训练不使用，也未提交到 Git |
| Heatmap `.npz` | 1475 | 旧标签数量，需重新生成 |
| `annotations.json` | 1475 帧 / 2950 ROI | 新增 141 帧尚未纳入 |
| split | 1180 / 147 / 148 帧 | train / val / test，重生成后会更新 |

GitHub 包含当前的 1616 张 RGB、BOP 姿态 JSON、3D 包围盒模型信息和已有角点标签。当前训练不读取 Depth，所以只进行角点训练时不用复制 Depth。

以下内容不在 GitHub 中，需要时单独复制：

- `checkpoints/`：训练产生的角点和 YOLO 权重。
- `yolov8n.pt`：YOLO 起始权重，可由 Ultralytics 重新下载。
- `head_left_rgb_raw.mp4`：真实测试视频。
- DINOv2 Torch Hub 缓存：只有离线运行时需手动复制。

当前还没有完成训练的 `checkpoints/dino_corner_net.pt`，也没有项目自己训练的 `dji_action4_yolo.pt`。

## 4. 游戏本推荐目录

以 Windows PowerShell 或 Anaconda Prompt 中的以下路径为例：

```text
D:\Code\Pose3d\
├── pose3d\
├── TripoSR\dataset\demo-bin-picking\
├── checkpoints\
├── outputs\
└── requirements-pose3d.txt
```

路径可以修改，但所有以 `python -m pose3d...` 开头的命令都应在项目根目录执行。建议使用纯英文、无空格路径，减少 Windows 工具链的路径问题。

## 5. Windows 环境配置

### 5.1 安装基础软件

1. 在 NVIDIA App 或 NVIDIA 官网安装当前显卡的最新 Studio Driver 或 Game Ready Driver，安装后重启。
2. 安装 Git for Windows。
3. 安装 Miniconda，并使用 Anaconda Prompt 执行后续命令。

先验证驱动能识别显卡：

```powershell
nvidia-smi
```

该命令应显示 NVIDIA GPU 型号、驱动版本和显存。当前项目不需要为 PyTorch 额外安装完整 CUDA Toolkit；PyTorch CUDA wheel 自带运行库，关键是 NVIDIA 驱动兼容。

### 5.2 克隆代码

```powershell
D:
mkdir Code
cd D:\Code
git clone https://github.com/songsong072300-arch/Pose3d.git
cd Pose3d
git rev-parse --short HEAD
```

最后一条命令应至少不早于基线 `f4719d7`。该仓库历史包含较大的数据文件，首次 clone 可能需要较长时间和约数 GB 空间。

### 5.3 创建 Python 环境

```powershell
conda create -n pose3d python=3.10 pip -y
conda activate pose3d
python -m pip install --upgrade pip
```

打开 PyTorch 官方安装页面 <https://pytorch.org/get-started/locally/>，选择：

```text
PyTorch Build: Stable
OS: Windows
Package: Pip
Language: Python
Compute Platform: 官方当前支持且与驱动兼容的 CUDA
```

把页面生成的 `pip install torch torchvision ...` 命令原样执行。不要根据 `nvidia-smi` 中的 `CUDA Version` 数字自行拼凑 wheel 地址，也不要在安装后再用其他 requirements 覆盖 `torch/torchvision`。

然后安装本项目普通依赖：

```powershell
python -m pip install -r requirements-pose3d.txt
```

如果后续要训练 YOLO 或运行完整推理链，再安装：

```powershell
python -m pip install ultralytics
```

### 5.4 验证 CUDA 环境

```powershell
python -c "import torch; print('torch:', torch.__version__); print('cuda build:', torch.version.cuda); print('available:', torch.cuda.is_available()); print('gpu:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'NONE')"
```

通过标准：

- `available: True`
- `gpu:` 后显示游戏本的 NVIDIA GPU
- `cuda build:` 不是 `None`

如果返回 `False`，先解决 PyTorch/driver 安装，不要继续正式训练。

## 6. DINOv2 预训练权重

`CornerHeatmapNet` 在初始化时通过 Torch Hub 加载 `facebookresearch/dinov2` 和 `dinov2_vits14_pretrain.pth`。

### 方式 A：游戏本可联网（推荐）

在项目根目录执行：

```powershell
python -c "from pose3d.common.model import CornerHeatmapNet; CornerHeatmapNet(); print('DINOv2 ready')"
```

首次执行会下载 DINOv2 代码和约 88 MB 的 Small 权重，之后使用本地缓存。

### 方式 B：游戏本离线

在 Mac 上把下列两项一起复制：

```text
~/.cache/torch/hub/facebookresearch_dinov2_main/
~/.cache/torch/hub/checkpoints/dinov2_vits14_pretrain.pth
```

放到 Windows 对应位置：

```text
C:\Users\<Windows用户名>\.cache\torch\hub\facebookresearch_dinov2_main\
C:\Users\<Windows用户名>\.cache\torch\hub\checkpoints\dinov2_vits14_pretrain.pth
```

离线时只复制 `.pth` 不够，`torch.hub.load()` 还需要 DINOv2 仓库代码。复制后仍用方式 A 的 Python 命令验证。

## 7. 训练前数据修复和检查

### 7.1 重新生成角点标签

克隆后必须执行一次，把新增 141 帧纳入 `annotations.json` 和数据划分：

```powershell
python TripoSR\generate_corner_heatmaps.py
```

预期输出：

```text
Generated 1616 labels: train=1292, val=161, test=163
```

注意：每帧有两个相机实例，因此上述划分在 `RoiCornerDataset` 中对应 `2584 / 322 / 326` 个 ROI 样本。当前训练集会从角点坐标在 ROI 尺度上现场构造 Heatmap，但这个生成脚本仍是更新 `annotations.json/splits.json` 的必需步骤。

### 7.2 检查文件数和划分

```powershell
python -c "from pathlib import Path; r=Path(r'TripoSR/dataset/demo-bin-picking'); print('RGB:', len(list((r/'train_pbr').glob('*/rgb/*.jpg')))); print('Heatmap:', len(list((r/'corner_labels/heatmaps').glob('*.npz'))))"
python -c "import json; from pathlib import Path; p=Path(r'TripoSR/dataset/demo-bin-picking/corner_labels/splits.json'); d=json.loads(p.read_text()); print({k:len(v) for k,v in d.items()})"
```

期望值为 `RGB: 1616`、`Heatmap: 1616`，split 为 `{'train': 1292, 'val': 161, 'test': 163}`。

如果 RGB 少于 1616，先在 Mac 完成数据同步，或重新 clone/pull。如果只有 Heatmap 少，重跑上一节的生成命令。

## 8. 先运行冒烟训练

正式训练前，先验证数据加载、DINOv2、CUDA、AMP、反向传播和 checkpoint 写入：

```powershell
python -m pose3d.train.train_heatmap `
  --root TripoSR\dataset\demo-bin-picking `
  --epochs 1 `
  --batch-size 2 `
  --workers 0 `
  --amp `
  --device cuda `
  --out checkpoints\smoke_dino_corner_net.pt
```

PowerShell 的换行符是反引号 `` ` ``。也可将命令写在同一行执行。通过标准：

- 开头显示 `device: cuda  amp: True`。
- 没有 CUDA out of memory、缺少图像或 DINOv2 下载错误。
- 完成 `epoch 001` 并生成 `checkpoints\smoke_dino_corner_net.pt`。

## 9. 正式训练

冒烟训练通过后执行：

```powershell
python -m pose3d.train.train_heatmap `
  --root TripoSR\dataset\demo-bin-picking `
  --epochs 30 `
  --batch-size 8 `
  --workers 4 `
  --amp `
  --device cuda `
  --out checkpoints\dino_corner_net.pt
```

在 Windows 上首次正式训练如果 DataLoader 子进程报错，将 `--workers 4` 改为 `--workers 0`。如果显存不足，依次把 `--batch-size` 从 `8` 降到 `4` 或 `2`。由于目前没有实现梯度累积，减小 batch 会改变优化器每个 epoch 的更新次数，需在训练结果中记录实际 batch size。

脚本只保存验证集 `corner_err` 创新低时的最佳 checkpoint，不是每个 epoch 都保存。checkpoint 包含：

```text
decoder          可训练解码器权重，不包含冻结的 DINOv2
epoch            最佳结果所在 epoch
val_loss         当时的验证损失
val_corner_err   当时的平均 ROI 像素角点误差
```

## 10. 日志字段解释

一行训练日志示例：

```text
epoch 001 train=... coarse=... fine=... val=... corner_err=...px
```

| 字段 | 含义 | 判断方法 |
|---|---|---|
| `train` | `coarse + 2.0 * fine` 的训练平均值 | 长期应总体下降 |
| `coarse` | 前景加权 Heatmap SmoothL1 | 观察热图监督是否收敛 |
| `fine` | spatial soft-argmax 坐标 SmoothL1 | 观察坐标级监督是否收敛 |
| `val` | 验证集总损失 | 用于发现过拟合，不用于保存判定 |
| `corner_err` | Heatmap argmax 到真值的平均 ROI 像素距离 | 越低越好，checkpoint 按该值保存 |

`corner_err` 是在 `224 x 168` ROI 空间中的误差，不是原始 `960 x 720` 画面的像素误差。

## 11. YOLO 和完整推理

角点网络训练可独立完成，不需要 YOLO。要在真实图像/视频上运行完整链路，还需要一个能检测相机机身的 YOLO 权重。

如需从合成标签训练 YOLO，先导出数据：

```powershell
python -m pose3d.train.export_yolo_labels `
  --root TripoSR\dataset\demo-bin-picking `
  --out checkpoints\yolo_dataset
```

当前导出脚本为节省磁盘使用符号链接。Windows 如果报创建链接权限错误，需开启 Windows “开发人员模式”或用管理员终端执行。YOLO 训练后把最佳权重放到：

```text
checkpoints\dji_action4_yolo.pt
```

单图完整推理：

```powershell
python -m pose3d.inference.predict_image `
  --image path\to\test.jpg `
  --detector checkpoints\dji_action4_yolo.pt `
  --corner-weights checkpoints\dino_corner_net.pt `
  --device cuda `
  --out outputs\corners.jpg
```

视频完整推理：

```powershell
python -m pose3d.inference.run_video `
  --video path\to\test.mp4 `
  --detector checkpoints\dji_action4_yolo.pt `
  --corner-weights checkpoints\dino_corner_net.pt `
  --device cuda `
  --out outputs\corners.mp4
```

## 12. 常见问题

| 现象 | 原因和处理 |
|---|---|
| `torch.cuda.is_available()` 为 `False` | 安装了 CPU wheel，或驱动与 wheel 不兼容。重新按 PyTorch 官方选择器安装 CUDA wheel。 |
| `CUDA out of memory` | 先把 batch size 从 8 降到 4/2，并确认已使用 `--amp`。 |
| DINOv2 首次加载失败 | 检查 GitHub/权重下载网络，或按第 6 节复制代码与权重两个缓存。 |
| `FileNotFoundError` 指向 RGB | 数据不完整或命令不在项目根目录执行；重做第 7.2 节检查。 |
| Windows DataLoader 子进程错误 | 将 `--workers 4` 改为 `--workers 0`。 |
| 训练中断后无法续训 | 当前 checkpoint 没有 optimizer/scheduler 状态，训练脚本也没有 `--resume`；现阶段需重新开始。 |
| 推理报缺少 `ultralytics` | 执行 `python -m pip install ultralytics`。 |
| 推理报缺少 detector 权重 | 项目尚未产生 `dji_action4_yolo.pt`，需先训练 YOLO 或提供已训练权重。 |

## 13. 迁移完成检查表

- [ ] `nvidia-smi` 能识别 NVIDIA GPU。
- [ ] `torch.cuda.is_available()` 返回 `True`。
- [ ] DINOv2-Small 能完成首次加载。
- [ ] RGB 和 Heatmap 都是 1616 份。
- [ ] 新 split 是 1292 / 161 / 163 帧。
- [ ] 1 epoch 冒烟训练成功生成 checkpoint。
- [ ] 30 epoch 正式训练已启动，并记录显卡、batch size 和日志。
- [ ] 最佳权重保存为 `checkpoints\dino_corner_net.pt`。
- [ ] 完整推理前已准备 `checkpoints\dji_action4_yolo.pt`。

## 14. 仍需注意的风险

1. 当前训练数据是合成图，真实视频中有手、腕带、线缆和大遮挡，合成到真实的领域差距仍需通过真实帧评估。
2. TripoSR 由单张正面图生成模型，背面屏幕是渲染脚本中的简化代理，与真机背面细节有差异。
3. 当前数据尚未补齐原计划的 2000 张；可以先用 1616 张训练基线，后续补渲染后必须再次重生成角点标签和 split。
4. 目前只有合成验证集。正式判断模型可用性时，应另建一个不参与训练的真实视频帧测试集。
