# pose3d 普通 Python 依赖（训练/推理角点热图网络）
#
# 先按 https://pytorch.org/get-started/locally/ 为当前 NVIDIA 驱动安装互相匹配的
# torch/torchvision CUDA wheel，然后再执行：
#   python -m pip install -r requirements-pose3d.txt
#
# 视频或双实例推理使用 YOLO 时再安装：
#   python -m pip install ultralytics
numpy>=1.26,<3
opencv-python-headless>=4.10,<5
pillow>=10,<13
scipy>=1.11,<2
